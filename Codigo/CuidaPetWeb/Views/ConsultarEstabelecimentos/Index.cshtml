@model IEnumerable<CuidaPetWeb.Models.ConsultarEstabelecimentosViewModel>

@{
    ViewData["Title"] = "Petshops";
}

<article class="d-flex justify-content-between align-items-center mb-3">
    <h2 class="fw-bold">@ViewData["Title"]</h2>
    <div class="d-flex gap-2 align-items-center">
        <div class="form-check form-switch ms-4 d-flex align-items-center">
            <input class="form-check-input" type="checkbox" id="togglePetshops" disabled>
            <label class="form-check-label me-2" for="togglePetshops">Petshops</label>
            <a asp-controller="ConsultarItens" asp-action="Index" class="btn btn-outline-primary">Itens</a>
        </div>
    </div>
</article>

<section>
    @if (Model != null && Model.Any())
    {
        <table class="table">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.First().Id)</th>
                    <th>@Html.DisplayNameFor(model => model.First().Nome)</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Nome</td>
                        <td>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm" title="Detalhes">
                                <i class="bi bi-eye-fill"></i> Details
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="d-flex justify-content-between p-2">
            <div class="p-2">
                <span class="fw-bold">Total de Petshops: </span> @ViewBag.TotalItems
            </div>
            <nav aria-label="Page navigation">
                <ul class="pagination">
                    @{
                        int TotalItems = ViewBag.TotalItems;
                        int pageSize = ViewBag.PageSize;
                        int currentPage = ViewBag.Page;
                        int totalPages = (int)Math.Ceiling((double)TotalItems / pageSize);

                        int maxPagesToShow = 5;
                        int startPage = Math.Max(1, currentPage - maxPagesToShow / 2);
                        int endPage = Math.Min(totalPages, startPage + maxPagesToShow - 1);

                        if (endPage - startPage < maxPagesToShow - 1)
                        {
                            startPage = Math.Max(1, endPage - maxPagesToShow + 1);
                        }
                    }

                    <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                        <a class="page-link text-primary" asp-action="Index" asp-route-page="@(currentPage - 1)" asp-route-pageSize="@pageSize" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                        </a>
                    </li>

                    @for (int i = startPage; i <= endPage; i++)
                    {
                        <li class="page-item @(i == currentPage ? "active" : "")">
                            <a class="page-link text-primary" asp-action="Index" asp-route-page="@i" asp-route-pageSize="@pageSize">@i</a>
                        </li>
                    }

                    <li class="page-item @(currentPage == totalPages ? "disabled" : "")">
                        <a class="page-link text-primary" asp-action="Index" asp-route-page="@(currentPage + 1)" asp-route-pageSize="@pageSize" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </div>
    }
    else
    {
        <div class="alert alert-info">Nenhum estabelecimento encontrado.</div>
    }
</section>